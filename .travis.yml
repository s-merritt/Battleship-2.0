language: java

cache:
  directories:
  - $HOME/.m2

  before_install:
  - pyenv install 3.6.0
  - pip install requests

script:
  - cd battleship/ && mvn test -B
  - bash <(curl -s https://codecov.io/bash)
  - python ../request.py